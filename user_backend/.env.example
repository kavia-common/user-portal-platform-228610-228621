# Express backend (default port per project: 3001)
PORT=3001
HOST=0.0.0.0

# CORS: React dev server origin
FRONTEND_ORIGIN=http://localhost:3000

# Sessions
SESSION_SECRET=change_me_to_a_long_random_string

# MySQL connection (project-standard DB port: 5001)
DB_HOST=localhost
DB_PORT=5001
DB_USER=appuser
DB_PASSWORD=dbuser123
DB_NAME=myapp
